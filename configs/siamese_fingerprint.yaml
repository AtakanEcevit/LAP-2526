# Siamese Network — Fingerprint Verification (SOCOFing)

model:
  type: siamese
  backbone: resnet
  embedding_dim: 128
  pretrained: true
  in_channels: 1

dataset:
  modality: fingerprint
  name: socofing
  root_dir: data/raw/fingerprints/SOCOFing

training:
  epochs: 10
  batch_size: 32
  lr: 0.0001
  weight_decay: 0.00001
  loss: bce                 # 'bce' (recommended) or 'contrastive'
  margin: 2.0               # only used if loss=contrastive
  scheduler: step
  lr_step: 20
  lr_gamma: 0.5
  patience: 15
  iterations_per_epoch: 100

evaluation:
  k_shots: [1, 3, 5, 10]

results_dir: results/siamese_fingerprint_socofing
