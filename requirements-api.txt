# Additional dependencies for the REST API server.
# Install on top of the base requirements.txt:
#   pip install -r requirements-api.txt

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
